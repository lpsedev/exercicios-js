const [a, b, c, d] = [15, 20, 3, 4]
const soma = a + b + c + d // Conhecida como operadores binários.
const subtracao = a - c
const multiplicacao = b * d
const divisao = a / d
const modulo = b % 2
console.log(soma, subtracao, multiplicacao, divisao, modulo)